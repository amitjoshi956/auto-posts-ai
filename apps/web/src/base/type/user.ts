// Re-exported from @autoposts/shared — kept here for backward compat with local imports
export type { UserProfile as User, LoginPayload as AuthPayload } from '@autoposts/shared';

// Signup payload (has fullName)
export type { SignupPayload } from '@autoposts/shared';
